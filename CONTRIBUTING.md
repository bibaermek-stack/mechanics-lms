# Механика AI LMS жобасына үлес қосу нұсқаулығы (Contributing Guide)

**Механика AI LMS** жобасына қызығушылық білдіргеніңізге рахмет! Бұл құжат жобаға өзгерістер енгізу, багтарды хабарлау және жаңа функционал қосу процесін реттейді.

---

## 📋 Әдеп кодексі (Code of Conduct)

Жобаға үлес қосушылар бір-біріне құрметпен қарауға, ашық және достық атмосфераны сақтауға міндетті. Кез келген дискриминацияға, дөрекілікке жол берілмейді.

---

## 🛠 Жұмысты бастау (Quick Start)

### 1. Репозиторийді форк жасау және клондау
1. GitHub-та жобаны өзіңізге **Fork** жасаңыз.
2. Жергілікті компьютерге клондаңыз:
   ```bash
   git clone https://github.com/<сіздің-username>/mechanics-lms.git
   cd mechanics-lms
   ```

### 2. Тәуелділіктерді орнату
```bash
npm install
```

### 3. Орта айнымалыларын саптау (Optional)
```bash
cp .env.local.example .env.local
```
> **Ескерту:** `.env.local` файлын толтырмасаңыз да, платформа толығымен жергілікті мок (mock) режимде жұмыс істей береді.

### 4. Әзірлеу серверін іске қосу
```bash
npm run dev
```
Браузерден `http://localhost:3000` ашыңыз.

---

## 🌿 Өзгерістер енгізу процесі (Workflow)

1. **Жаңа ветка (branch) ашыңыз:**
   ```bash
   git checkout -b feature/жаңа-функционал-аты
   # немесе
   git checkout -b fix/баг-аты
   ```

2. **Код стандартын сақтаңыз:**
   - Интерфейстегі барлық мәтіндер **тек қазақ тілінде** болуы тиіс.
   - Код пен түсініктемелер (comments) ағылшын тілінде жазылады.
   - TypeScript типтерін толық көрсетіңіз (`any` типін қолданбауға тырысыңыз).
   - TailwindCSS арқылы Glassmorphism стиль жүйесін сақтаңыз.

3. **Commit хабарламаларының форматы (Conventional Commits):**
   ```text
   feat: жаңа викторина компонентін қосу
   fix: AI тьютордың жауап қайтару қатесін түзету
   docs: README файлына нұсқаулық қосу
   style: дашборд дизайнын жақсарту
   refactor: competency бағалау логикасын оңтайландыру
   ```

4. **Кодты тексеру:**
   Коммит жасамас бұрын жобаның қатесіз құрастырылатынына көз жеткізіңіз:
   ```bash
   npm run build
   ```

5. **Pull Request (PR) жіберу:**
   - Өзгерістерді форк жасалған репозиторийге жүктеңіз:
     ```bash
     git push origin feature/жаңа-функционал-аты
     ```
   - GitHub-та негізгі `main` веткасына **Pull Request** ашыңыз.
   - PR сипаттамасында қандай өзгерістер енгізілгенін және не үшін жасалғанын жазыңыз.

---

## 🌐 Қазақ тіліндегі мазмұн саясаты

Платформа магистрлік диссертация аясында жасалғандықтан, мына ережелерді ұстанған жөн:
- Барлық жаңа батырмалар, мәзірлер, хабарламалар, викторина сұрақтары мен ойын сөздері әдеби, грамматикалық тұрғыдан дұрыс қазақ тілінде жазылуы керек.
- Механика пәнінің терминологиясы (мысалы: *кинематика, динамика, статика, импульс, энергетикалық сақталу заңдары*) стандартты оқулықтарға сай қолданылуы тиіс.

---

## 🏆 Contributors тізіміне қалай қосылуға болады?

PR мақұлданғаннан кейін, сіз автоматты түрде немесе `CONTRIBUTORS.md` файлына өзіңізді қосу арқылы үлес қосушылар тізіміне енгізілесіз.

Рахмет және сәттілік! 🚀
